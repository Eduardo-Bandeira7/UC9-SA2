let = document.getElementById("campo-email")

let = document.getElementById("campo-senha")

function enviarLogin() {
    let emailDigitado = email.value
    let senhaDigitada = password.value;
    console.log(loginDigitado)
}